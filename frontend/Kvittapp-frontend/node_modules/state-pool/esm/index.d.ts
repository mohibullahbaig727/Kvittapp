import State, { createState, DerivedState, createDerivedState } from './State';
import Store, { createStore } from './Store';
import useReducer from './useReducer';
import useState from './useState';
export { State, createState, Store, createStore, useState, useReducer, DerivedState, createDerivedState, };
declare const _default: {
    State: typeof State;
    createState: typeof createState;
    Store: typeof Store;
    createStore: typeof createStore;
    useState: typeof useState;
    useReducer: typeof useReducer;
    DerivedState: typeof DerivedState;
    createDerivedState: typeof createDerivedState;
};
export default _default;
